import { StaffLoginForm } from "@/components/staff-login-form";

export default function DeliveryLoginPage() {
  return <StaffLoginForm title="Bite Corner — Delivery Agent Login" redirectTo="/delivery" role="DELIVERY_AGENT" />;
}
